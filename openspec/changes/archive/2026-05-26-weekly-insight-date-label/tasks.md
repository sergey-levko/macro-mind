## 1. Frontend — InsightPanel date label

- [x] 1.1 In `Coach.tsx` `InsightPanel`, replace `` `Week of ${insight.periodStart}` `` with `formatWeekLabel(insight.periodStart)` so the saved weekly insight card shows a formatted week range (e.g. "May 19 – 25" or "This week (May 19 – 25)") instead of a raw ISO date
